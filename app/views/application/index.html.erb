<b>Search Post Tags:</b></br>

<%= form_tag '/', method: :get do %>
<%= text_field_tag :q, params[:q] %>
<%= submit_tag 'Search' %>
<% end %></br></br>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%= link_to post.id, post_path(post) %>
    </li>
    <br>
  <% end %>
</ul>
